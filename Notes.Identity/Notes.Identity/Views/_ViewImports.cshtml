@using Notes.Identity.Models
@addTagHelper "*, Microsoft.AspNetCore.Mvc.TagHelpers"

@* 

    @using Notes.Identity.Models: Директива @using в шаблонах Razor используется 
    для подключения пространства имен в текущий контекст шаблона, что дает возможность 
    обращаться к классам этого пространста имен без необходимости полного их объявления. 
    В данном случае подключается пространство имен Notes.Identity.Models, что, вероятно, 
    содержит определения моделей для системы идентификации.

    @addTagHelper "*, Microsoft.AspNetCore.Mvc.TagHelpers": Директива @addTagHelper включает 
    все помощники тегов (TagHelpers) в указанном пространстве имен. Помощники тегов используются 
    для серверного рендеринга HTML, позволяя авторам представлений создавать визуальные 
    компоненты (HTML и CSS), используя синтаксис Razor и C#. В этом случае включаются все 
    помощники тегов из пространства имен Microsoft.AspNetCore.Mvc.TagHelpers, включающее 
    стандартные помощники тегов, предоставляемые ASP.NET Core.

*@